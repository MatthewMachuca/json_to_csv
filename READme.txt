The program accepts the file name of a Complex nested JSON, flattens the values, and writes it to a CSV file.

The use case is to provide all attributes for a given endpoint with examples, path, data type
